#include <iostream>
#include <exception>
#include <server.hpp>
#include <signal.h>
#include"http.hpp"
int main()
{
    auto empty = new std::vector<std::string>;
    npos = *empty;
    try
    {
        signal(SIGINT,destory_sfd);
        init_server(8010);
    }
    catch(std::exception e)
    {
        printf("WHAT THE F**K!?");
    }
}





